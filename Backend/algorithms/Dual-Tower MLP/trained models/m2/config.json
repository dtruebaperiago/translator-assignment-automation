{
  "model_name": "m2",
  "description": "Batch change, num_workers > 0",
  "loss_criterion": "MSELoss",
  "hyperparameters": {
    "embedding_dim": 64,
    "batch_size": 64,
    "max_num_epochs": 50,
    "learning_rate": 0.001,
    "weight_decay": 0.0001,
    "lr_factor": 0.5,
    "lr_patience": 3,
    "lr_min": 1e-6,
    "early_stop_patience": 7,
    "num_workers": 0
  },
  "notes": "Batch change, num_workers > 0."
}