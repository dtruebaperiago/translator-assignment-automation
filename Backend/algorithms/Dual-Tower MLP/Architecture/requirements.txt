# ============================================================
# IDISC Translation Task Assignment System — Dependencies
# ============================================================
# Install everything with:
#   pip install -r requirements.txt
#
# NOTE (Dual-Tower graph export only):
#   torchviz also needs the Graphviz *system binary* to render PNGs.
#   Download from https://graphviz.org/download/ and add to PATH.
# ============================================================

# --- Core Deep Learning ---
torch>=2.0.0

# --- Model Inspection & Visualization ---
torchinfo>=1.8.0       # Rich layer summary table (shape-aware)
torchviz>=0.0.2        # Computational graph → PNG export
graphviz>=0.20         # Python wrapper for Graphviz binaries (used by torchviz)
